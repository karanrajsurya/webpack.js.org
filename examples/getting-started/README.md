# Getting Started

## How to use

Run the following commands in the root directory.

```bash
<!-- This project currently has peer dependency conflicts with npm v7+, so "--legacy-peer-deps" is required for installation. -->
npm install --legacy-peer-deps #
npm run build
npm run start
```
